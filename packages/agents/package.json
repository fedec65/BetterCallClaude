{
  "name": "@bettercallclaude/agents",
  "version": "1.0.0",
  "description": "BetterCallClaude Agent Framework for Swiss Legal Research",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "agents",
    "swiss-law",
    "legal-research",
    "mcp-client"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
